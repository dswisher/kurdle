namespace Kurdle.Processors.Blog
{
    public class BlogLayoutProcessor
    {
        // TODO - Take a blog post that has been rendered to HTML, apply a layout, and write to disk
        // TODO - should this just be an HTML layout processor? Take an HTML fragment document and apply layout and write to disk?
    }
}
